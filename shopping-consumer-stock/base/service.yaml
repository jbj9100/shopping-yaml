apiVersion: v1
kind: Service
metadata:
  name: shopping-dev-consumer-stock-service
  namespace: shopping-dev-consumer-stock
spec:
  selector:
    app: shopping-dev-consumer-stock
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: metrics