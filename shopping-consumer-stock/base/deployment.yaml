apiVersion: apps/v1
kind: Deployment
metadata:
  name: shopping-dev-consumer-stock
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: shopping-dev-consumer-stock
  template:
    metadata:
      labels:
        app: shopping-dev-consumer-stock
      annotations:
        instrumentation.opentelemetry.io/inject-python: "true"  
    spec:
      containers:
      - name: shopping-dev-consumer-stock
        image: registry.example.com/shopping-dev-consumer-stock/consumer-stock:v1
        envFrom:
        - configMapRef:
            name: shopping-dev-consumer-stock-config
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
