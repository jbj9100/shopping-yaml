apiVersion: v1
kind: Service
metadata:
  name: shopping-dev-backend-service
  labels:
    app: shopping-dev-backend
spec:
  selector:
    app: shopping-dev-backend
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http