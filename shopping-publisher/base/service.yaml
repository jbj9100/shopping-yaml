apiVersion: v1
kind: Service
metadata:
  name: shopping-dev-publisher-service
  namespace: shopping-dev-publisher
spec:
  selector:
    app: shopping-dev-publisher
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: metrics